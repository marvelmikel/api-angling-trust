export const setForm = (state , form ) => {
    state.form = form
}

export const setHasRegistered = (state, has_registered) => {
    state.has_registered = has_registered
}

export const setHasSubmitted = (state, has_submitted) => {
    state.has_submitted = has_submitted
}

export const setRegistrationIntention = (state, registration_intention) => {
    state.registration_intention = registration_intention
}
